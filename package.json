{
  "name": "keypair.me",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel --dist-dir docs src/index.html",
    "build": "prettier --write . && rm -rf docs && parcel build --dist-dir docs --public-url ./ src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.2.0",
    "prettier": "2.5.1"
  }
}
